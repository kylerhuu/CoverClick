# Public API origin baked into production extension builds (no secrets).
# Safe to commit — visible in the built extension and network requests anyway.
VITE_COVERCLICK_API_ORIGIN=https://coverclick-production.up.railway.app
