# Build-time override for mock mode defaults in `src/lib/storage.ts`.
# - true: force mock responses (no network), regardless of saved settings
# - false: respect the "Use mock generation" toggle in Options
#
# VITE_USE_MOCK=true
