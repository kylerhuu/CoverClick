# Extension build-time defaults (Vite). Copy to `.env` or `.env.production`.

# Required for production builds that talk to your hosted API (no trailing slash).
# VITE_COVERCLICK_API_ORIGIN=https://api.yourdomain.com

# Optional: force mock generation in the built extension
# VITE_USE_MOCK=true
