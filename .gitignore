node_modules
dist
.DS_Store
*.log
.env
.env.local
# Root .env.production is committed (public VITE_COVERCLICK_API_ORIGIN only).
# Server secrets stay in server/.env (never commit).
coverclick-extension.zip
